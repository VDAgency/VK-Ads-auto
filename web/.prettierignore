# Артефакты сборки и зависимости.
out
.next
node_modules

# Статика прежнего фронта: отдаётся как есть, со своим форматированием.
# Прогон prettier по ней — шум в diff и риск сломать разметку, которую
# проверяют tests/test_web_static.py.
public

# Генерируются инструментами, руками не редактируются.
package-lock.json
next-env.d.ts
