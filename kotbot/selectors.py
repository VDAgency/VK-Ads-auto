"""Селекторы и URL кабинета ads.vk.ru (VK Реклама) — ground truth разведки 2026-07-17.

Реальная цель браузерной автоматизации — интерфейс ads.vk.ru, НЕ сайт kotbot.
Первичный вход через VK ID captcha-gated: его делает оператор руками, а сессия
переиспользуется через storage_state. Поэтому автоматизируем то, что идёт ПОСЛЕ
входа: проверка залогиненности + мастер кампании «подписчики» + остановка.

Все селекторы вынесены сюда именованными константами и сгруппированы по экранам,
чтобы при переезде вёрстки VK правки были в одном месте. Часть селекторов
(шаги 2 и 3 мастера) на разведке ещё не снята — помечены TODO(live).
"""

from __future__ import annotations

# --- Базовые URL ------------------------------------------------------------------

# Корень кабинета VK Рекламы.
ADS_BASE = "https://ads.vk.ru"
# Прямой вход в мастер создания рекламной кампании (ad_plan).
WIZARD_URL = "https://ads.vk.ru/hq/new_create/ad_plan"

# --- Экран входа VK ID (для справки; сам вход делает оператор вручную) -------------

# Поле телефона: маска, вводить посимвольно через type_text.
VKID_PHONE_INPUT = 'input[name="login"][type="tel"]'
# Шесть ячеек одноразового кода.
VKID_OTP_CELL = 'input[name="otp-cell"]'
# Поле пароля (если вход по паролю, а не по коду).
VKID_PASSWORD_INPUT = 'input[name="password"]'

# --- Маркеры залогиненности кабинета ads.vk.ru ------------------------------------

# Есть в тексте кабинета авторизованного пользователя.
LOGGED_IN_MARKER_TEXT = "Создать кампанию"
# Есть на маркетинговой главной для анонима (кнопка регистрации).
LOGGED_OUT_MARKER_TEXT = "Регистрация"

# --- Мастер кампании «подписчики»: Режим эксперта → Целевые действия ----------------

# Переключатель на расширенный режим настройки кампании.
CAMPAIGN_MODE_EXPERT_TEXT = "Режим эксперта"
# Цель кампании — целевые действия.
OBJECTIVE_TARGET_ACTIONS_TEXT = "Целевые действия"
# Карточка «что рекламируем»: сообщества/приложения VK, ОК, Mini Apps.
PROMOTE_VK_CARD_TEXT = "ВКонтакте, ОК, Mini Apps"
# Вкладка объекта продвижения — сообщество/группа/профиль.
OBJECT_TAB_COMMUNITY_TEXT = "Сообщество, группа или профиль"
# Целевое действие «подписчики»; для сообщества проставляется по умолчанию,
# в флоу лишь проверяем его наличие в тексте экрана.
TARGET_ACTION_SUBSCRIBE_TEXT = "Подписка на сообщество"
# Кнопка перехода к следующему шагу мастера.
CONTINUE_BUTTON_TEXT = "Продолжить"

# TODO(live): точный селектор поля дневного бюджета на разведке 2026-07-17 не снят.
# Best-effort до дозаписи — текстовое поле рядом с меткой «Бюджет»; уточнить на
# живом запуске (возможна маска — тогда ввод через type_text вместо fill).
BUDGET_INPUT_TODO = 'input[name*="budget" i]'

# Шаги мастера (левый рельс навигации):
#   "Настройка кампании" / "Группы объявлений" / "Объявления".
# TODO(live): селекторы шага 2 (таргетинг: гео/возраст/пол) — дозаснять на запуске.
# TODO(live): селекторы шага 3 (креатив: заголовок/текст/медиа) — дозаснять на запуске.
